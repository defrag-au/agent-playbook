# direnv: enter flake.nix's devshell on cd, so `direnv exec . cargo test` works in a
# sandbox (where `nix develop` cannot reach the daemon socket). Needs `direnv allow .`
# once, from an unsandboxed shell.
use flake
