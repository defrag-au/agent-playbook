# <Addendum title>

<!--
Copy to models/<target>/addenda/<name>.md and list it in the overlay's `addenda:`.

Harness mechanics only: the names of the tools available, how a harness phrases its nudges
and what they actually mean, what a sandbox refuses and the invocation that works instead,
how output is rendered and what is silently dropped.

Behavioural rules go in rules/ — even when only one model needs them, put them there with
`targets: <name>` so they are visible as rules rather than hidden in an overlay.

The first heading is demoted one level at render time, so it sits under the rules rather than
competing with the document title.
-->
