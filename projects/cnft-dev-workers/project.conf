project: cnft-dev-workers
path: ~/code/defrag/cnft.dev-workers
org: defrag
languages: rust
default_target: claude-code
# The repo directory is cnft.dev-workers; this name is the playbook's handle for it.
# What is genuinely local to this repo lives in rules/ beside this file — everything
# shared with the rest of the ecosystem lives in rules/org/defrag/.
