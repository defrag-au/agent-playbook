target: generic
model: any
harness: any
include:
exclude:
emphasis:
addenda:
default_file: AGENTS.md
# last reviewed: 2026-09-21
# Baseline target. Emits the resolved rule set unmodified, with no preamble and no
# addenda. Use this to see what a project actually resolves to before layering a
# model on top:
#   playbook compose --project shared-crates --target generic
